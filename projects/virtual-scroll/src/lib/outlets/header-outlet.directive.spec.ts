import { HeaderOutletDirective } from './header-outlet.directive';

describe('HeaderOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderOutletDirective();
    expect(directive).toBeTruthy();
  });
});
